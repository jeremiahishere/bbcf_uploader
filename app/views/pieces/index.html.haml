%h1 Listing pieces

= link_to 'New Piece', new_piece_path
%table
  %tr
    %th Name
    %th Composer
    %th Inventory/Version
    %th
    %th
    %th
    %th

  - @pieces.each do |piece|
    %tr
      %td= piece.name
      %td= piece.composer_and_arranger
      %td= piece.inventory_and_version
      %td= link_to 'Parts', parts_for_piece_path(piece)
      %td= link_to 'Show', piece
      %td= link_to 'Edit', edit_piece_path(piece)
      %td= button_to 'Destroy', piece, :confirm => 'Are you sure?', :method => :delete
